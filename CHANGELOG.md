# 版本更新说明

## v4.1.0 (2026-08-06)

### 新增功能

- **排盘记录功能**：基于 localStorage 自动保存每次排盘记录，不影响起卦流程
  - 排盘完成（interpret 执行后）自动保存，无需手动操作
  - 首页 header 区域新增"排盘记录"入口按钮
  - 记录列表显示：卦名、动爻/静卦、求测内容、起卦时间、干支信息、保存时间
  - 点击记录查看详情：完整爻象表（纳甲/六亲/六神/旺衰/世应/状态）、排盘详情、解卦分析
  - 支持单条删除、一键清空（含确认弹窗）
  - 支持导出全部记录为 JSON 文件备份
  - 最多保存 200 条记录，超出自动裁剪最早的
  - 弹层 UI 适配移动端响应式布局

---

## v4.0.0 (2026-08-06)

### 重大架构升级

- **集成 lunar-javascript 库替换自写农历/干支计算**：
  - 引入 `lunar.js`（lunar-javascript v1.7.7），替代手写的 `LUNAR_INFO` 数组和自定义转换函数
  - 农历↔公历转换：使用 `Solar.fromYmd()` / `Lunar.fromYmd()` 精确计算，替代手写 offset 累加算法
  - 闰月判断：使用 `LunarYear.fromYear().getLeapMonth()` 替代位运算 `LUNAR_INFO & 0xf`
  - 年柱干支：使用 `getYearInGanZhiExact()` 精确立春时刻计算，替代近似"2月4日"边界
  - 月建地支：使用 `getMonthZhiExact()` 精确节气时刻计算，替代硬编码近似节气日期表
  - 月柱干支：使用 `getMonthInGanZhiExact()` 直接获取，替代手写五虎遁 `getMonthTianGan()`
  - 日干支：使用 `getDayInGanZhi()` 精确计算，替代 1900 基准日 offset 算法
  - 删除 `LUNAR_INFO` 数组(201元素)、`lYearDays`、`leapDays`、`monthDays` 等底层函数

### 精度提升

- **立春边界**：旧代码近似为2月4日，新代码精确到立春时刻（如2026年立春在2月4日具体时分，2月4日0时仍属上一年）
- **节气边界**：旧代码使用硬编码近似日期（如立秋≈8月8日），新代码精确到每节气的天文时刻
- **月建切换日**：修正了节气当天可能误判月建的问题（如2026年8月8日才进入申月，而非旧代码的8月8日当天即判申月）

### 测试验证

- 更新 `test_cases.js`：干支计算测试用例改用 lunar-javascript 精确数据，**413 项全部通过**
- `test_divination.js`：排盘逻辑测试不受影响，**145 项全部通过**
- 验证多组日期对照：2026-08-04=六月廿二、2026-08-06=六月廿四、2024-02-05=甲辰年立春后等

---

## v3.2.0 (2026-08-05)

### 新增功能

- **求测内容快捷标签**：在求测内容输入框下方新增 12 个快捷标签，点击即可自动填入对应求测内容，免去手动输入
  - 标签涵盖：求财谋事、工作升职、婚姻感情、健康疾病、考试功名、出行安全、家宅风水、行人归期、官司诉讼、失物寻人、怀孕生产、自身年运
  - 标签样式与水墨风格统一，支持悬停高亮和点击反馈

### Bug 修复

- **用神识别关键词缺失**（`autoDetectSpirit` 函数）：
  - 修复"求测出行平安"被误判为子孙爻的问题 — "平安"原先仅在子孙类关键词中，现同时在世爻类中匹配，出行+平安双关键词命中世爻
  - 修复"求测行人归期"返回 null 的问题 — 新增"行人""归期"到世爻类关键词
  - 修复"求测失物寻人"返回 null 的问题 — 新增"失物""寻人"到世爻类关键词

### 测试验证

- 新增 `test_cases.js` 综合测试文件，直接从 `knowledge.js` 加载真实数据，共 **410 项断言全部通过**
  - 覆盖：八宫卦序(64卦完整性)、世应位置(8宫×8卦)、纳甲(8纯卦全爻)、六亲分配、六合六冲卦、旬空、六神起法、神煞(驿马/桃花/日禄/卦身)、干支计算(年月日时)、旺衰(四季)、十二长生、进退神、墓库、变卦纳甲、用神识别(12类)、4个完整排盘场景
- 更新 `test_divination.js` 同步修正八宫卦序和世应表数据，**145 项全部通过**

### 版本号对齐

- 将代码中 `APP_VERSION` 从 v2.2.0 更新为 v3.2.0，与 Git 提交历史对齐

---

## v3.1.0 (2026-08-05)

- 首页添加参考书籍标注（《增删卜易》《火珠林》）
- 全面移动端自适应优化（多断点响应式布局）

## v3.0.0 (2026-08-04)

- 水墨风格 UI 改版：宣纸纹理背景、楷体标题、墨色层次配色、朱砂红重点色

## v2.2.2 (2026-08-04)

- 同步 index.html 为最新版本
- 优化公历农历切换按钮样式

## v2.2.1 (2026-08-04)

- 去掉日期显示区的"农历："标签文字

## v2.2.0 (2026-08-04)

- 新增公历/农历输入切换功能

## v2.1.1 (2026-08-04)

- 修复农历数据：替换 LUNAR_INFO 完整数组(1900-2100)

## v2.1.0 (2026-08-04)

- 首页添加农历月份时间显示

## v2.0.0 (2026-08-04)

- 添加页面版本号显示
